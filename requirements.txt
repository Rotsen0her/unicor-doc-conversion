# Motores de conversión (instalar primero — arrastran su propio árbol de
# dependencias con pydantic/httpx; el resto se resuelve alrededor de esto)
docling
markitdown[pdf]

# Ya llega transitivo vía docling (docling-slim) — explícito porque
# app/engines/docling_engine.py importa PIL directo para exportar imágenes.
pillow

# FastAPI y servidor
fastapi
uvicorn[standard]
python-multipart
pydantic-settings

# Utilidades
python-dotenv

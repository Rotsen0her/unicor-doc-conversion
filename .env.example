# ==========================================
# AUTH INTERNA (OBLIGATORIO en producción)
# ==========================================
# Compartida con backend_web_bot — mismo valor en ambos .env.
# Genera con: python -c "import secrets; print(secrets.token_hex(32))"
INTERNAL_API_KEY=CAMBIA_ESTO_valor_aleatorio_compartido_con_backend_web_bot

# ==========================================
# LÍMITES
# ==========================================
MAX_FILE_SIZE_MB=25

# ==========================================
# SERVIDOR
# ==========================================
HOST=0.0.0.0
PORT=8100
DEBUG=False
ENABLE_DOCS=False

LOG_LEVEL=INFO
